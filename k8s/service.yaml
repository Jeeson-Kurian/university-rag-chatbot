apiVersion: v1
kind: Service
metadata:
  name: chatbot-service
  namespace: university-chatbot
spec:
  type: NodePort
  selector:
    app: university-rag-chatbot
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080